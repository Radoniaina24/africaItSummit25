import { Metadata } from "next";
import React from "react";
export const metadata: Metadata = {
  title: "Health | Africa It Summit",
  description: "",
  // other metadata
};

export default function Health() {
  return <div className="mt-36">Health</div>;
}
